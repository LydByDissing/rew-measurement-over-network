# REW Pi Audio Receiver - Environment Configuration
# Copy this file to .env and customize for your setup

# Pi identification
PI_HOSTNAME=rew-pi-1
VERSION=latest

# Audio configuration
AUDIO_DEVICE=default
# AUDIO_DEVICE=hw:0,0          # Specific hardware device
# AUDIO_DEVICE=pulse           # Use PulseAudio

# Network configuration
RTP_PORT=5004                  # RTP audio streaming port
HTTP_PORT=8080                 # HTTP status/API port

# Logging configuration
LOG_LEVEL=INFO                 # DEBUG, INFO, WARNING, ERROR

# Receiver command line arguments
RECEIVER_ARGS=--device default --rtp-port 5004 --http-port 8080 --verbose

# Docker Compose profiles to enable
# Uncomment to enable system monitoring
# COMPOSE_PROFILES=monitoring

# Advanced audio settings (uncomment if needed)
# PULSE_SERVER=unix:/run/pulse/native
# ALSA_PCM_DEVICE=0
# ALSA_PCM_SUBDEVICE=0

# Container resource limits (adjust for your Pi model)
# Pi Zero/1: memory: 128M, cpus: '0.5'
# Pi 2/3:    memory: 256M, cpus: '1.0' 
# Pi 4/5:    memory: 512M, cpus: '2.0'